# ==============================================
# BACKEND/AGENT CONFIGURATION (Required)
# ==============================================

# API Keys - Provide at least one
# OpenAI API key for GPT models
OPENAI_API_KEY=

# Anthropic API key for Claude models (recommended)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# LLM Provider - Which AI provider to use
# Options: "anthropic" or "openai"
LLM_PROVIDER=anthropic

# Model ID - Specific model to use
# Examples:
#   - Anthropic: claude-3-5-sonnet-20241022, claude-3-opus-20240229
#   - OpenAI: gpt-4, gpt-3.5-turbo
MODEL_ID=claude-3-5-sonnet-20241022

# Framework - Agent framework to use
# Options: "agno"
FRAMEWORK=agno

# Database Path - Path to SQLite database file
# Example: ./data/peupajoh.sqlite3
DB_PATH=./data/peupajoh.sqlite3

# ==============================================
# FASTAPI CONFIGURATION (Optional - Has Defaults)
# ==============================================

# Server host address (default: 0.0.0.0)
# Use 0.0.0.0 to listen on all interfaces, or 127.0.0.1 for localhost only
HOST=0.0.0.0

# Server port (default: 8000)
PORT=8000

# Auto-reload on code changes (default: true)
# Set to false in production
RELOAD=true

# CORS allowed origins (default: *)
# Use comma-separated list for multiple origins
# Examples:
#   - Allow all: *
#   - Specific domains: https://yourdomain.com,https://app.yourdomain.com
CORS_ORIGINS=*

# CORS allow credentials (default: true)
CORS_ALLOW_CREDENTIALS=true

# CORS allowed methods (default: *)
# Examples: GET,POST,PUT,DELETE or *
CORS_ALLOW_METHODS=*

# CORS allowed headers (default: *)
CORS_ALLOW_HEADERS=*

# ==============================================
# API METADATA (Optional - Has Defaults)
# ==============================================

# API title shown in documentation (default: Peupajoh API)
API_TITLE=Peupajoh API

# API description shown in documentation
API_DESCRIPTION=Food tracking and nutrition analysis API

# API version (default: 1.0.0)
API_VERSION=1.0.0

# API URL prefix (default: /api/v1)
API_PREFIX=/api/v1

# Swagger UI documentation URL (default: /api/docs)
DOCS_URL=/api/docs

# ReDoc documentation URL (default: /api/redoc)
REDOC_URL=/api/redoc